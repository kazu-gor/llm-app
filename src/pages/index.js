import React from 'react';
import Layout from '../components/Layout';
import Chat from '../components/Chat';

const Home = () => {
  return (
    <Layout>
      <Chat />
    </Layout>
  );
};

export default Home;
